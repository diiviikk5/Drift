# Drift Chrome Extension

Lightweight browser extension for recording browser tabs and window streams with the Drift engine.

---

## 🚀 Features

- **Tab Capture**: Capture current browser tab with system audio.
- **Popup UI**: One-click recording triggers and resolution presets.
- **WebCodecs Export**: Instant client-side MP4 and WebM generation.
